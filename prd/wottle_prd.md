# Wottle MVP – Product Requirements Document

Wottle is a competitive **2-player real-time word duel** merging word-search gameplay with chess-clock tension and spatial tile-freezing strategy. This PRD provides exact gameplay rules, scoring formulas, architecture, and UX requirements for implementation of the MVP with clear, testable specifications.

## 1. Gameplay Mechanics & Rules

### 1.1 Board and Letter Layout
- **Board Size:** The game board is a 16×16 grid of letter tiles (256 tiles total). Each tile initially shows a random letter, generated by a weighted distribution (see **Board Generation** below). All letters of the alphabet appear at least once on the board.
- **Letter Distribution:** Letters are placed according to their frequency in the language (not by point value). This ensures common letters appear more often, while high-value letters are rarer. For MVP (Icelandic), every valid letter (including special characters like **ð, þ, æ**) will appear at least once.
- **Dictionary & Words:** Words are validated against an **Icelandic nouns** dictionary (~18,000 words in nominative singular form). Words are recognized case-insensitively, but language-specific characters are treated as distinct letters (e.g. **Ð** vs **D**). The architecture is designed to support plugging in different dictionaries for other languages in future releases.

### 1.2 Word Formation Rules
- **Valid Words:** Players form words by swapping letters (see **Moves** below). A valid word is any sequence of **3 or more letters** found in a straight line on the grid. All 8 directions are allowed: horizontal (left-to-right **and** right-to-left), vertical (top-down **and** bottom-up), and diagonals in all four orientations. Words cannot wrap around edges of the board.
- **Contiguous Path:** The letters of a word must lie in contiguous adjacent tiles in one direction with no gaps. Only unfrozen (movable) tiles can form new words (see **Frozen Tiles** below), though a player’s *own* frozen tiles may be part of a new word if they line up appropriately.
- **Word Validation:** After each move, the game server checks all 8 directions from the swapped positions to identify any new contiguous sequences of ≥3 letters that form valid dictionary words. Word matching is case-insensitive and respects diacritical marks in the current language.
- **Word Scoring Uniqueness:** Each distinct word can contribute to a player’s score **only once per match**. If the same exact word (same letter sequence) is formed by the **same player** again in a later move, it will be identified but will **not** award points a second time. (This prevents exploits like re-forming a word repeatedly for points.) Each newly discovered word by a player is only scored the first time that player finds it.

### 1.3 Move Mechanics
- **Swapping Letters:** A **move** consists of selecting any two letter tiles on the board (they do **not** need to be adjacent) and swapping their positions. This is the only move action in the game. After a swap, the resulting board is automatically checked for new words formed by that swap.
- **Controls:** On **desktop**, a swap can be done by clicking one tile and then another, or by dragging the first tile onto the second. On **mobile**, swapping is done by tapping one tile and then another, and the swapp happens instantly when the second tile is tapped.
- **Move Resolution:** When the player confirms a swap, a brief animation (~150–250ms) plays to swap the tiles. Immediately after, any new word(s) formed by that swap are highlighted on the board and *claimed* by the player. The server calculates the score for that move and updates the player’s total (see **Scoring** below). The move is then finalized: claimed tiles freeze in place, and control passes to the opponent.

### 1.4 Turn Structure & Time Control
- **Simultaneous Turn Start:** At the **start of the game**, both players begin with an active timer (“chess clock”). Each player has **5:00 minutes** of base time, with a **+3 second** increment added after each move (5+3 time control). **Both clocks start running when the game begins**, allowing *either player to make the first move* (a simultaneous planning phase). The server resolves who moved first, and stops the clock of the first-mover. The second player's clock keeps running until they make a move, after which the players take turns making moves. In the case of both players making the first move simultaniously, or nearly at the same time such that it can't be determined fairly who moved first, it is decided by random which player moved first.
- **Turn-by-Turn Flow:** Players alternate turns after the first move. Only one move executes at a time. When one player’s move resolves, control passes to the opponent.
- **Clock Behavior:** Each player’s clock counts down **only** during their turn (except for the initial move when both player's clocks count down). When a move is made, that player’s clock pauses and the opponent’s continues. Each completed move adds +3 seconds to the mover’s clock.
- **Fair Play Safeguards:**
  - If a player’s clock expires before completing 10 moves, the opponent may still finish their remaining moves.
  - All timing is enforced server-side for fairness and to prevent client manipulation.

### 1.5 Frozen Tiles & Territory Control
- **Tile Freezing:** Whenever a word is discovered, **all letters in that word become frozen**. A frozen tile is locked in place for the rest of the game. Neither player can move that tile again. This tile becomes that player’s “territory”.
- **Visual Marking:** Frozen tiles are marked with a colored overlay (40% opacity) or border. Shared letters (if both players claim overlapping words) display a dual-color pattern. Colors must remain high-contrast and readable.
- **Gameplay Effects:**
  - Frozen tiles cannot be swapped.
  - Opponent cannot form new words that use opponent’s frozen tiles.
  - Player can use own frozen tiles to extend new words.
- **Safeguard:** Server enforces **≥24 unfrozen tiles** after any move (to avoid lockups).

### 1.6 Game Progression & End Conditions
- **Move Limit:** 10 moves per player (20 total).
- **Game Ends When:**
  - Both players reach 10 moves, or
  - Time expires for both players, or
  - One player resigns.
- **Winner:** Highest score wins. Tiebreaker: most frozen tiles. If tied, draw.

## 2. Scoring System

### 2.1 Formula
```
Turn Score = Σ(Base Word Scores) + Σ(Length Bonuses) + Multi-Word Combo Bonus
```
- **Base Word Score:** Sum of letter values in all new words.
- **Length Bonus:** (word_length − 2) × 5 points per word.
- **Multi-Word Bonus:**
  - 1 word → +0
  - 2 words → +2
  - 3 words → +5
  - 4+ words → +7 + (n−4)
- **Unique Scoring:** Each word counted once per player per match.
- **Overlap Handling:** Shared letters count for each distinct word.

### 2.2 Implementation
- Scoring runs server-side.
- Letter values stored per language version.
- Word re-use by same player yields 0.
- Turn delta displayed to both players (e.g. `+18 letters, +3 length, +2 combo`).

## 3. Game Modes & Player Flow

### 3.1 Lobby & Matchmaking
- **Modes:**
  - **Ranked:** Elo-based, ±200 expanding +50 every 10s.
  - **Casual:** Unrated; flexible pairing.
  - **Challenge:** Direct invite.
- **Flow:** Lobby → Select Mode → Match Found → Game Start.

### 3.2 Game Setup
1. Assign White/Black roles randomly.
2. Generate board using weighted letter distribution + seed words.
3. Initialize 5+3 clocks.
4. Display initiative message.
5. Begin simultaneous phase.

### 3.3 In-Game Flow
1. Active player swaps two letters.
2. Server validates move, finds words, calculates score.
3. Freeze claimed tiles.
4. Broadcast new board state + scores.
5. Switch turn or resolve simultaneous moves.

### 3.4 Post-Game Flow
- Show final scores, word highlights, and winner.
- Update Elo (ranked mode).
- Offer rematch or return to lobby.

## 4. Board Generation & Word Seeding
- **Weighted Distribution:** Based on letter frequency for current language.
- **Completeness:** Ensure all alphabet letters appear ≥1×.
- **Seed Words:** Embed ≥6 potential words solvable within ≤2 swaps.
- **Anti-Lock:** Spread seeds to prevent clustering.
- **Validation:** Minimum 20 possible words, ≥24 movable tiles.
- **Reproducibility:** Deterministic seed (match_id).
- **Performance:** <200ms generation time.

## 5. Dictionary & Localization
- **Icelandic MVP:** 18k nouns (nominative, singular).
- **Validation Engine:** In-memory Trie or hash set; O(1) lookup.
- **Multi-Language Support:** Configurable via dictionary ID and letter values.
- **Unicode Handling:** NFC normalized, language-specific.
- **Localization:** UI and messages translatable (EN/IS at launch).

## 6. Technical Architecture

### 6.1 Frontend
- **Stack:** Next.js (React), TypeScript, Tailwind CSS.
- **Realtime:** Supabase Realtime / Socket.io.
- **Animations:** CSS transforms; 60 FPS.

### 6.2 Backend
- **Stack:** Supabase (Postgres + Functions).
- **Responsibilities:**
  - Validate swaps & scoring.
  - Manage turn order & clocks.
  - Broadcast real-time updates.
  - Store results.
- **Data Model:**
  - `users` (profiles, Elo)
  - `matches` (seed, status, players)
  - `boards` (grid)
  - `moves` (coords, words, delta)
  - `dictionaries` (per language)
  - `ratings` (history)
- **Scalability:** Postgres for persistence.
- **Security:** JWT auth; WSS required; server-authoritative logic.

## 7. UI / UX

### 7.1 Layout
**Desktop:**
```
┌──────────────────────────────┐
│ Opponent [04:23] |  Score 340│
│──────────────────────────────│
│         16×16 GRID           │
│──────────────────────────────│
│ You [04:47] | Score 385 | M7 │
└──────────────────────────────┘
```
### 7.2 Visual Feedback
- Frozen tiles: colored border + tint.
- New words: flash highlight.
- Invalid swap: shake/red border.
- Score delta: transient popup near score.

## 9. Non-Functional Requirements
- **Latency:** <200ms move RTT.
- **Validation:** <50ms per move.
- **Match Length:** ~10–20min.
- **Reconnect:** ≤10s resume window.
- **Testing:** Unit (Trie, Scoring, Clock), Integration (Match Flow).
- **Security:** Sanitized input, HTTPS/WSS.

## 10. Success Metrics
| Metric | Target |
|---------|--------|
| Matchmaking Time | <10s |
| Word Validation | <50ms |
| Day-1 Retention | ≥30% |
| Avg Session | 6–10min |
| Fairness (Win Split) | 49–51% |
| Abandon Rate (<4 moves) | <8% |
