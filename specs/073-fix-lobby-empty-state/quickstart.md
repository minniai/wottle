# Quickstart: Verify Lobby Empty State

1. Run the focused lobby unit test.
2. With `overview.lastMatch` set to `null`, verify the first-match sentence is visible and no field or rules figure exists.
3. With a completed `lastMatch`, verify the review link and band-map preview remain present.
4. Inspect at 901–1167px and phone width. Confirm no last-game content exceeds the page width.
