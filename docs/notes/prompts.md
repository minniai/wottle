# Prompts Log

- Analyse the current state of the code for Wottle and the state of all the past development milestones with the aim of doing a alpha release of the MVP to a limited audience. Analyse what needs to be added or fixed to make this production release possible. Suggest the next feature to implement for Wottle for the alpha release, if it seems more appriopriate. Give some reasoning for your suggestion.
- **Scoring:** The scoring should find all possible words to be scored in the location of the swapped character in all four directions and then it score the words that give the highest scoring words for the user. Each move can score at most four words, two in either swap location. For each character it can find a vertical words and a horizontal words. It should then choose the words found the highest scoring words.
- Analyse the Wottle prd @docs/prd_and_requirements/wottle_prd.md, specs and related documents and suggest the next feature and improvements to implement after 010-word-discovery-highlights. Make a suggestion for the next feature that is not too big or takes to long to implement but is definitely moving the development towards the end goal of an amazing playable word game.
- The word-scoring as currently implemented is not correct. I want you to analyse the code, specs, and descriptions thoroughly and deeply to make sure it is sound and according to spec. Then write a clear and concise description of exactly how words are scored, and what the rules used. The report should be aimed at humans to understand and be able to understand the rules.
- There are two problem word-discoveries on this board I want you to analyse.
  1. In location 0,3 there is KRÁ from bottom to top. The user swapped letters such that Á was placed in location 0,0, and thus forming the word KRÁ, read from bottom to top. The word-resolution process did not score the word after the move, which it should have.
  2. The horizontal letter-combination ÍIÁV was scored in location 5,5, does not seem to form a legal word. Analyse and suggest the rational behand the scoring if valid or if it is an error.
- Words should be valid in both raeding directions, from left-to-right, right-to-left, top-to-bottom and bottom-to-top. Add that to word validation and scoring rules.
- Change the game scoring rules and frozen tiles. Frozen tiles should only mean that they cannot be moved/swapped by either player, it should not mean that a word cannot be scored by a player if it contains any tile frozen by their opponent. However if you add a letter to a word "owned" by the opponent, you do score for the added letters that form the word as well as for the length of the newly scored word. Say that the red player owns the word TAÐI horizontally in location 3,1 and the swaps the letters A in location 2,1 and R in location 2,2, thus forming the word RATAÐI which is a valid word. Then blue player should score for the letters RA (which become blue, while letters TAÐI remain red), but the length of the word scored by Blue would be 6, i.e. the length of the word RATAÐI.
- In this the screenshot, the words HLÉ and LIÐS were discovered and scored, but the two words have adjacent letters HL and LI f.ex. which do not form a valid word sho the word should not have scored. Examine and suggest a solution.
- The scoring is still wrong, in that adjacent letters that don't form a word are skored. In the screenshot the word TÍK and FÓLK adjacent such that the last letter K in TÍK is right abouve K in fólk, forming the word KK which is not a valid word. Analyse the code, and specs and write a precise specification in clear English of how the scoring rules are.
- The word-finding rules is still not correct as words are scored that do contain letters adjacent to letters that do not form a word, e.g. if the word "hundur" is horizontal in line one at coordinate (1,1) and then a pleyr forms the word "hestur" is horizontal in line 2 at coordinate (2,1) then "hestur" should not score as the letters h at (1,1) and h in (2,1) are adjacent horizontally and do not form a horizontal word as "hh" is not a word.
- The timer doesn't stop when player makes a move. It should be such that as soon as a player makes a move in a round their timer should stop the countdown (and the timer of the player who hasn't made a move yet keeps ticking. As soon as the second player makes their move in the round, the round is complete. In other words, in each round the timer starts when the round starts and ticks while the players are are thinking about the move and stops when they make their move.
- Analyse the Wottle prd @docs/wottle_prd.md and related documents and suggest the next feature and improvements to implement after 003-word-engine-scoring. It is important to make the UI and design start showing as the current implementation is hardly playeable as the UI elements don't fit the screen and are just for testing the backend infrastructure. Make a suggestion for the next feature that is not too big or takes to long to implement but is definitely moving the development towards the end goal of an amazing playable word game.
- Analyse the Wottle prd @docs/wottle_prd.md and related documents and suggest the next feature and improvements to implement after 006-match-completion. Make a suggestion for the next feature that is not too big or takes to long to implement but is definitely moving the development towards the end goal of an amazing playable word game.
  Add config options to the game such as: the word length, the number of rounds, the time per round, left-to-right, right-to-left, top-to-bottom, bottom-to-top, etc. For the MVP the config options can be set in code, and are not required in a UI Change the scoring and word validation rules to be more in line with Scrabble. We will not use diagonal words, only horizontal and vertical words. Another key change is that words have to valid in every direction at every coordinate, meaning, if two letters adacent to each other are not part of a valid word in all four directions (horizontal, vertical, and the two diagonals), then the word is not valid.
