# Prompts Log

- In this the screenshot, the words HLÉ and LIÐS were discovered and scored, but the two words have adjacent letters HL and LI f.ex. which do not form a valid word sho the word should not have scored. Examine and suggest a solution.
- The scoring is still wrong, in that adjacent letters that don't form a word are skored. In the screenshot the word TÍK and FÓLK adjacent such that the last letter K in TÍK is right abouve K in fólk, forming the word KK which is not a valid word. Analyse the code, and specs and write a precise specification in clear English of how the scoring rules are.
- The word-finding rules is still not correct as words are scored that do contain letters adjacent to letters that do not form a word, e.g. if the word "hundur" is horizontal in line one at coordinate (1,1) and then a pleyr forms the word "hestur" is horizontal in line 2 at coordinate (2,1) then "hestur" should not score as the letters h at (1,1) and h in (2,1) are adjacent horizontally and do not form a horizontal word as "hh" is not a word.
- The timer doesn't stop when player makes a move. It should be such that as soon as a player makes a move in a round their timer should stop the countdown (and the timer of the player who hasn't made a move yet keeps ticking. As soon as the second player makes their move in the round, the round is complete. In other words, in each round the timer starts when the round starts and ticks while the players are are thinking about the move and stops when they make their move.
- Analyse the Wottle prd @docs/wottle_prd.md and related documents and suggest the next feature and improvements to implement after 003-word-engine-scoring. It is important to make the UI and design start showing as the current implementation is hardly playeable as the UI elements don't fit the screen and are just for testing the backend infrastructure. Make a suggestion for the next feature that is not too big or takes to long to implement but is definitely moving the development towards the end goal of an amazing playable word game.
- Analyse the Wottle prd @docs/wottle_prd.md and related documents and suggest the next feature and improvements to implement after 006-match-completion. Make a suggestion for the next feature that is not too big or takes to long to implement but is definitely moving the development towards the end goal of an amazing playable word game.
  Add config options to the game such as: the word length, the number of rounds, the time per round, left-to-right, right-to-left, top-to-bottom, bottom-to-top, etc. For the MVP the config options can be set in code, and are not required in a UI
  Change the scoring and word validation rules to be more in line with Scrabble. We will not use diagonal words, only horizontal and vertical words. Another key change is that words have to valid in every direction at every coordinate, meaning, if two letters adacent to each other are not part of a valid word in all four directions (horizontal, vertical, and the two diagonals), then the word is not valid.
